<?xml version="1.0" encoding="utf-8"?>

<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:desktop="http://schemas.microsoft.com/appx/manifest/desktop/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap desktop rescap">

  <Identity
    Name="LotrasoftInc.CaffeinePro"
    Publisher="CN=83954149-93B6-44D3-A8BC-084E5568F786"
    Version="3.0.0.0" />

  <Properties>
    <DisplayName>Caffeine Pro</DisplayName>
    <PublisherDisplayName>Lotrasoft Inc.</PublisherDisplayName>
    <Logo>Images\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <!--
    Listed one by one rather than left as "x-generate". The UI is translated with .NET satellite
    assemblies, which MakePri does not see, so a generated list would come out as en-US only - and
    for a packaged app that list is also what Windows resolves CurrentUICulture against, which
    would pin every machine to English no matter its display language. The first entry is the
    fallback. Keep in sync with LocalizationService.Languages.
  -->
  <Resources>
    <Resource Language="en-US"/>
    <Resource Language="fr"/>
    <Resource Language="es"/>
    <Resource Language="de"/>
    <Resource Language="pt"/>
    <Resource Language="ja"/>
    <Resource Language="zh-Hans"/>
    <Resource Language="ar"/>
    <Resource Language="fa"/>
  </Resources>

  <Applications>
    <Application Id="App"
      Executable="$targetnametoken$.exe"
      EntryPoint="$targetentrypoint$">
      <uap:VisualElements
        DisplayName="Caffeine Pro"
        Description="Keeps Windows awake. Stops the screen from dimming, the PC from sleeping, and your status from going idle, for as long as you choose."
        BackgroundColor="transparent"
        Square150x150Logo="Images\Square150x150Logo.png"
        Square44x44Logo="Images\Square44x44Logo.png">
        <uap:DefaultTile Wide310x150Logo="Images\Wide310x150Logo.png" />
      </uap:VisualElements>
      <Extensions>
        <!--
          Start with Windows. A packaged app cannot use the HKCU\...\CurrentVersion\Run key
          (writes to it are virtualized away), so the option is driven through this startup
          task instead. Enabled="false" so installing does not opt the user in; the app calls
          StartupTask.RequestEnableAsync when the setting is switched on. The TaskId must stay
          in sync with Routines.StartupTaskId.
        -->
        <desktop:Extension
          Category="windows.startupTask"
          Executable="CaffeinePro\CaffeinePro.exe"
          EntryPoint="Windows.FullTrustApplication">
          <desktop:StartupTask
            TaskId="CaffeineProStartupTask"
            Enabled="false"
            DisplayName="Caffeine Pro" />
        </desktop:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
